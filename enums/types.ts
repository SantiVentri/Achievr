export interface GoalType {
    id: string;
    icon: string;
    title: string;
    short_description: string;
    creator_id: string;
    is_done: boolean;
    updated_at: string;
    created_at: string;
}